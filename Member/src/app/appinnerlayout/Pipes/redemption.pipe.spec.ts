import { RedemptionPipe } from './redemption.pipe';

describe('RedemptionPipe', () => {
  it('create an instance', () => {
    const pipe = new RedemptionPipe();
    expect(pipe).toBeTruthy();
  });
});
